## 0.0.1

* Provides a wrapper on Dio that uses `package:azure_application_insights/azure_application_insights.dart` to send network request telemetry.
* Button Widgets, that show a spinner during progress.
